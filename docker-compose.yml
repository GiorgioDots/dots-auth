services:
  dots-meals: 
    image: ghcr.io/giorgiodots/dots-meals:prod
    container_name: dotsmealsapi
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data